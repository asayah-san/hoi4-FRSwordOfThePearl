on_actions = {

    # ROOT is the winner
    # FROM gets annexed
    on_annex = {
        effect = {
            if = { 
                limit = { 
                    ROOT = { tag = UFC }
                    FROM = { tag = ROOT }
                } 
                every_state = {
                    limit = { is_core_of = PHI }
                    add_core_of = UFC
                }
            }

            if = {
                limit = {
                    ROOT = { tag = PHI }
                    FROM = { tag = UFC }
                }
                every_state = {
                    limit = { is_core_of = UFC }
                    add_core_of = PHI
                    remove_core_of = UFC
                }
            }
        }
    }
}